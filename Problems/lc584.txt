create schema if not exists lc584;

drop table if exists lc584.customer;

create table lc584.customer (
    id         int primary key,
    name       varchar(50),
    referee_id int
);

insert into lc584.customer values
(1, 'will', null),
(2, 'jane', null),
(3, 'alex', 2),
(4, 'bill', null),
(5, 'zack', 1),
(6, 'mark', 2);

select * from lc584.customer;

select name
from lc584.customer
where referee_id is null or referee_id <> 2;